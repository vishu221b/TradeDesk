"""TradeDesk — a provider-agnostic LLM tool-use agent for small-business
operations (runs on Claude, GPT, Gemini, or local Ollama), served as a
multi-user, database-backed full-stack application."""

__version__ = "2.0.0"
